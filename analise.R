m <- c(1, 2, 3, 4, 5)

# Outras Linhas

  # Calculando a média
  media_do_M <- mean(m)
  
  # Mostrando o resultado
  print(media)
  
  # Criando um gráfico simples
  plot(m,
       type = "b",
       main = "Valores do vetor m",
       xlab = "Índice",
       ylab = "Valor")